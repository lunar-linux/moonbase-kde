depends latex-xft-fonts
depends python
depends ImageMagick
depends qt6-svg

optional_depends boost \
                 "--without-included-boost" "--with-included-boost" \
                 "use with system or included boost,${PROBLEM_COLOR}Say Yes for system${DEFAULT_COLOR}"

optional_depends aspell \
                 "--with-aspell" \
                 "--without-aspell" \
                 "for dictionary support"

optional_depends hunspell \
                 "--with-hunspell" \
                 "--without-hunspell" \
                 "for hunspell support"

optional_depends enchant \
                 "--with-enchant" \
                 "--without-enchant" \
                 "for generic spellcheck support"

optional_depends texlive "" "" "Not needed to build, but useful"             
